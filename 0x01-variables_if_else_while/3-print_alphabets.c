#include <stdio.h>

/**
 * main - yannzogoroje ubushize
 * Description: 'just a functu=ion'
 * Return: Always zero (Success)
*/

int main(void)
{
	 
	for (ch = 'A'; ch <= 'Z'; ch++)
	{

        	putchar(ch);

    	}


        for (ch = 'a'; ch <= 'z'; ch++)

	{

	        putchar(ch);

	}


	putchar('\n');
	return (0);

}
