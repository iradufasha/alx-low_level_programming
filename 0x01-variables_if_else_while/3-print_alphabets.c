#include <stdio.h>

/**
 * main - yannzogoroje ubushize
 * Description: 'just a functu=ion'
 * Return: Always zero (Success)
*/

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}

	for (ch = 'A'; ch <= 'Z'; ch++)
	{
		putchar(ch);
	}


	putchar('\n');
	return (0);

}
